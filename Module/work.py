# syntax from module_name import fun_name,variable_name,class_name
from module import add as a #as key word is used for making alisting of any class or funtion
from module import sub as b #as key word is used for making alisting of any class or funtion
x=int(input("enter your no, "))
y=int(input("enter your no, "))
print(a(x,y))
print(b(x,y))